
---
STRICT ADDENDUM (runtime):

1) NEWS:
- Используй только то, что дано в секции "=== [NEWS] ===". Не выдумывать.
- В "news_context" верни 0–3 коротких пункта, если заголовки релевантны.

2) MARKET CONTEXT:
- Суммаризируй BTC/ETH строго по снапшоту (цены, 24h%), без диапазонов и домыслов.

3) TP/SL (мягкие ориентиры):
- Базово: SL ≈ 1% от входа; TP1 ≈ +1%; TP2 ≈ +2%.
- Допустимы небольшие сдвиги для совпадения с локальными структурами (EMA/VRVP/уровни), при этом R:R ≥ 2.0.
- TP3 только при высокой уверенности; иначе не нужен.

4) STYLE:
- Если NEWS дал релевантные заголовки — "news_context" не пустой.
- Избегай общих фраз, конкретика только там, где влияет на сетап.

---
NEWS & CONTEXT RULES (do not ignore):
- Заполняй "news_context" 1–3 пунктами, если в SNAPSHOT есть NEWS.
  Формат каждого пункта: "[impact: pos|neg|neutral] Короткий заголовок (1 строка)".
  Выбирай только релевантное выбранному активу или общерыночное (ETF, SEC, ликвидации, макро).
- "market_context": 1–2 короткие фразы. Обязательно упомяни BTC/ETH 24h % (словами) и вывод "risk-on/neutral/risk-off → что это значит для альтов".
- Не выдумывай новости. Используй только то, что в SNAPSHOT NEWS.
- В сигнале избегай общих фраз: дай конкретный смысловой вывод из NEWS/контекста, но кратко.
